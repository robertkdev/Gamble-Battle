# Legacy Probes (Deprecated)

These probes are kept temporarily while we migrate to the Role Matrix Probe.

- IdentityProbe.tscn (and script)
  - Deprecated in favor of `tests/rga_testing/validation/RoleMatrixProbe.tscn`.
  - Path updates preserved so it still runs if needed; please prefer the new probe.

Once feature parity is confirmed and reports stabilize, this folder will be removed.
